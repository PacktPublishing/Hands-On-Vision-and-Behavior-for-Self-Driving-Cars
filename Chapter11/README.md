# Chapter 11 Mapping with Ouster and Cartographer

## Clone the folder for this chapter which includes the submodule `ouster_example_cartographer`

To initialize the submodule run:
* `git submodule init`
* `git submodule update`
